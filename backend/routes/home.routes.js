// lam
// renders timeline of posts 
// get posts (findAll)

const posts = require("../controllers/post.controller")

module.exports = app => {
    router.get("/home")
    app.use("/tutorials", router)
}
